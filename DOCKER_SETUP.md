# RailsプロジェクトのDocker化手順

## 1. プロジェクトの準備
- GitHubリポジトリの作成
  - [ihatov08/rails7_docker_template](https://github.com/ihatov08/rails7_docker_template)を使用し、プロジェクトの雛形を作成しました。このテンプレートのおかげで、Docker環境のセットアップがわずかな手順で完了しました。
  - `rails-docker`という名前でリポジトリを作成しました。

## 2. Dockerfileの作成
- **ベースイメージの選定**
  - Rubyのバージョンを指定しました。例えば、`ruby:3.2.2`を使用しました。これにより、アプリケーションが必要とするRubyの環境が整い、依存関係の問題を回避できました。
  
- **必要なパッケージのインストール**
  - PostgreSQLクライアントやその他の依存関係をインストールしました。以下のように記述しました。
    ```dockerfile
    RUN apt-get update -qq && \
        apt-get install -y --no-install-recommends \
        build-essential \
        nodejs \
        yarn \
        libpq-dev
    ```
  - これにより、アプリケーションが必要とするライブラリやツールがインストールされ、開発環境が整いました。

- **アプリケーションコードのコピー**
  - アプリケーションのコードをコンテナにコピーしました。これにより、コンテナ内でアプリケーションを実行できるようになりました。

- **アセットのプリコンパイル**
  - 本番環境用にアセットをプリコンパイルしました。これにより、アプリケーションのパフォーマンスが向上し、ユーザーに対して迅速なレスポンスを提供できます。

## 3. docker-compose.ymlの作成
- **PostgreSQLの設定**
  - PostgreSQLのバージョンを12に指定しました。これにより、データベースの互換性が保たれ、安定した動作が期待できました。
  
- **Webアプリケーションサービスの定義**
  - 環境変数の設定やボリュームの設定を行いました。ホストとコンテナのファイルシステムを同期させることで、開発がスムーズになりました。これにより、コードの変更が即座に反映され、効率的な開発が可能でした。

- **データベースサービスの定義**
  - PostgreSQLのサービスを定義しました。これにより、アプリケーションがデータベースにアクセスできるようになりました。

## 4. コンテナのビルドと起動
- `docker-compose up`コマンドを実行して、コンテナをビルドし、起動しました。これにより、アプリケーションとデータベースが同時に立ち上がりました。
- コンテナの状態を確認し、Railsサーバーが正常に起動していることを確認しました。これにより、アプリケーションが正しく動作しているかを確認できました。
  - コンテナの状態を確認するには、`docker-compose ps`コマンドを実行しました。

## 5. データベースのセットアップ
- **データベースの作成**
  - 以下のコマンドを実行してデータベースを作成しました。
    ```bash
    docker-compose run web rails db:create
    ```

- **データベースへの接続確認**
  - データベースへの接続が正常に行えるか確認しました。これにより、アプリケーションがデータベースと正しく連携できることを確認しました。
  - データベースの確認は、`docker-compose run web rails db:migrate`コマンドを実行して、マイグレーションが正常に行われるかを確認しました。

## 6. アプリケーションへのアクセス
- ブラウザで`http://localhost:3000`にアクセスし、アプリケーションが正常に動作しているか確認しました。
- タスク管理のアプリケーションが立ち上がるので、タスクを作成して、アプリケーションが正しく動作しているか確認しました。

## 7. READMEの更新
- 環境構築手順をREADMEに記述しました。
  - Git cloneから始まる手順
  - Dockerのインストール方法
  - コンテナのビルドと起動方法
  - アプリケーションへのアクセス方法
  - データベースの設定方法

## 8. まとめ
- **Dockerfileを利用したコンテナのビルド**
  - アプリケーションの依存関係を定義し、環境を整えました。

- **Docker Composeを利用したコンテナの起動**
  - 複数のサービスを一括で管理し、簡単に起動できました。
